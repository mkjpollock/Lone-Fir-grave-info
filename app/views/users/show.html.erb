<h1><%= @user.email %></h1>

<% if @user.roles.first != nil %>
	<h3><%= @user.roles.first.name %></h3>
<% end %>

<#% if can? :manage, :all %>
	<#%= form_for @user, user_path(@user) do |f| %>

<#% end %>